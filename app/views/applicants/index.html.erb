<div class='container'>
  <h1 class='mb-5'><%= @vacancy.title %></h1>

  <table class="table">
    <thead class='table-active th-text-color'>
      <tr>
        <th>Nome</th>
        <th class='text-center'>Enviado em</th>
        <th class='text-center'>Ações</th>
      </tr>
    </thead>

    <tbody class='text-secondary'>
      <% @applicants.each do |applicant| %>
        <tr>
          <td class="align-middle"><%= applicant.name %></td>
          <td class="align-middle text-center"><%= applicant.created_at.strftime('%d/%m/%Y') %></td>
          <td class="align-middle text-center">
            <% if applicant.curriculum.attached? %>
              <a href="<%= url_for(applicant.curriculum) %>" target="_blank" class="text-secondary text-decoration-none" aria-label="Baixar currículo">
                <i class="fas fa-file-download fa-fw"></i>Baixar
              </a>
            <% else %>
              <span class="text-muted">Sem currículo</span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
    
  </table>
</div>